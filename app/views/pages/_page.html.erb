<div class="subpage">
  <%= link_to(image_tag(page.images.first.public_filename(:medium)), page_path(page.permalink)) unless page.images.first.nil? %>
  <h2><%= link_to page.title, page_path(page.permalink) %></h2>
  <%= page.description %>
</div>
